{
  "devDependencies": {
    "dir-archiver": "^2.1.0"
  },
  "scripts": {
		"bundle": "dir-archiver --src . --dest ../redirect-after-logout.zip --exclude .DS_Store .git  .gitattributes .github .gitignore README.md composer.json composer.lock node_modules vendor package-lock.json package.json phpcs.xml.dist"
	}
}
